<?xml version="1.0" encoding="UTF-8"?><rss xmlns:dc="//purl.org/dc/elements/1.1/" xmlns:content="//purl.org/rss/1.0/modules/content/" xmlns:atom="//www.w3.org/2005/Atom" version="2.0" xmlns:media="//search.yahoo.com/mrss/"><channel><title><![CDATA[Ankit Sharma - nezubn]]></title><description><![CDATA[Thoughts, life and universe.]]></description><link>nezubn.com/</link><image><url>nezubn.com/favicon.png</url><title>Ankit Sharma - nezubn</title><link>nezubn.com/</link></image><generator>Ghost 3.40</generator><lastBuildDate>Mon, 25 Jan 2021 09:58:17 GMT</lastBuildDate><atom:link href="nezubn.com/author/ankit/rss/" rel="self" type="application/rss+xml"/><ttl>60</ttl><item><title><![CDATA[From code to production]]></title><description><![CDATA[<!--kg-card-begin: markdown--><h1 id="tableofcontents">Table of Contents</h1>
<ol>
<li><a href="#org94aa6ec">laptop setup and system design for deep learning</a>
<ol>
<li><a href="#org441c7ea">Software design principles</a></li>
<li><a href="#org592b6d3">Points to ponder</a></li>
<li><a href="#orgdc36e10">Pipeline</a></li>
<li><a href="#orge0dcd01">References</a></li>
</ol>
</li>
<li><a href="#org487f124">Project structure of Deep Learning project</a>
<ol>
<li><a href="#orgc9e1d24">Project Structure</a></li>
<li><a href="#org52a11dd">Object Oriented Programming in python</a>
<ol>
<li><a href="#org2372f66">Abstraction</a></li>
<li><a href="#org56aa7e3">Static and class methods</a></li>
</ol>
</li>
<li><a href="#org786b4f5">Type checking</a></li>
<li><a href="#org15dfd66">Documentation</a></li>
<li><a href="#orgc95c330">References</a></li>
</ol>
</li>
<li><a href="#org1dc87e3">Unit Testing</a>
<ol>
<li><a href="#org5c09b22">Why unit testing?</a></li>
<li><a href="#orgbb414ab">Basics of unit testing</a></li></ol></li></ol>]]></description><link>nezubn.com/from-code-to-production/</link><guid isPermaLink="false">6007b35656c39f44086f9b03</guid><dc:creator><![CDATA[Ankit Sharma]]></dc:creator><pubDate>Wed, 20 Jan 2021 04:37:20 GMT</pubDate><content:encoded><![CDATA[<!--kg-card-begin: markdown--><h1 id="tableofcontents">Table of Contents</h1>
<ol>
<li><a href="#org94aa6ec">laptop setup and system design for deep learning</a>
<ol>
<li><a href="#org441c7ea">Software design principles</a></li>
<li><a href="#org592b6d3">Points to ponder</a></li>
<li><a href="#orgdc36e10">Pipeline</a></li>
<li><a href="#orge0dcd01">References</a></li>
</ol>
</li>
<li><a href="#org487f124">Project structure of Deep Learning project</a>
<ol>
<li><a href="#orgc9e1d24">Project Structure</a></li>
<li><a href="#org52a11dd">Object Oriented Programming in python</a>
<ol>
<li><a href="#org2372f66">Abstraction</a></li>
<li><a href="#org56aa7e3">Static and class methods</a></li>
</ol>
</li>
<li><a href="#org786b4f5">Type checking</a></li>
<li><a href="#org15dfd66">Documentation</a></li>
<li><a href="#orgc95c330">References</a></li>
</ol>
</li>
<li><a href="#org1dc87e3">Unit Testing</a>
<ol>
<li><a href="#org5c09b22">Why unit testing?</a></li>
<li><a href="#orgbb414ab">Basics of unit testing</a></li>
<li><a href="#org54688cb">Unit tests in Python</a></li>
<li><a href="#orgf42bfcf">Mocking</a></li>
<li><a href="#orgb4cdacd">Test coverage</a></li>
<li><a href="#orgc0eb724">Test examples</a></li>
<li><a href="#org0e0c7fa">Integration</a></li>
<li><a href="#org400fe94">Conclusion</a></li>
<li><a href="#orgd3f9aaf">References</a></li>
</ol>
</li>
<li><a href="#orgbb96761">Logging and Debugging</a>
<ol>
<li><a href="#orge9c000a">Debug deep learning</a></li>
<li><a href="#orgeb14bd2">Logging</a></li>
<li><a href="#org3e41322">Tensorflow logging and debugging</a></li>
</ol>
</li>
</ol>
<p><a id="org94aa6ec"></a></p>
<h1 id="donelaptopsetupandsystemdesignfordeeplearning">DONE <a href="https://theaisummer.com/deep-learning-production/">laptop setup and system design for deep learning</a></h1>
<p>Suggested Tools: Git, Github, PyCharm, terminal, slack, zenhub, conda, zoom</p>
<p>What distinguishes a good engineer from a great engineer?</p>
<ul>
<li>His ability to plan and design the system before even touching the keyboard. Carefully thinking about the<br>
architecture, making informed decisions, discussing it with other engineers, and documenting things can save<br>
a lot of time beforehand. A whole lot of time. Trust me on this. I’m guilty of this more times than I’m proud<br>
to admit. That’s why, this time we will do it the right way. Thus, it turns out it won’t be such a short<br>
parenthesis!</li>
</ul>
<p>Questions in our mind?</p>
<ul>
<li>What will happen if more than 1 users ask the model at the same time?</li>
<li>What will happen if for some reason the model crashes?</li>
<li>If it contains a bug we haven’t previously seen?</li>
<li>If the model is too slow and the user has to wait for too long to get a response?</li>
<li>And how can we update our model?</li>
<li>Can we retrain it on new data?</li>
<li>Can we get some feedback if the model performs well?</li>
<li>Can we see how fast or slow the model is?</li>
<li>How much memory does it use?</li>
<li>Where do we store the model weights?</li>
<li>What about the training data?</li>
<li>What if the user sends a malicious request instead of a proper image?</li>
</ul>
<p><a id="org441c7ea"></a></p>
<h2 id="softwaredesignprinciples">Software design principles</h2>
<ol>
<li>Separation of concerns<br>
The system should be modularized into different components with each component being a separate maintainable,<br>
reusable and extensible entity.</li>
<li>Scalibility<br>
The system needs to be able to scale as the traffic increases</li>
<li>Reliability<br>
The system should continue to be functional even if there is software of hardw   are failure</li>
<li>Availability<br>
The system needs to continue operating at all times</li>
<li>Simplicity<br>
The system has to be as simple and intuitive as possible</li>
</ol>
<p><a id="org592b6d3"></a></p>
<h2 id="pointstoponder">Points to ponder</h2>
<ul>
<li>Each user sends a request to our backend. To be able to handle all the simultaneous requests, first we need<br>
a <code>Load Balancer</code> (LB) to handle the traffic and route the requests to the proper server instance.</li>
<li>We maintain more than one instance of the model in order to achieve both scalability and availability. Note<br>
that these might be physical instances, virtual machines or docker containers and they are organized by a<br>
distributed system orchestrator (such as Kubernetes)</li>
<li>As you can probably see, each instance may have more than one threads or parallel processes. We need that to<br>
make sure that the model is fast enough to return a response in real time.</li>
<li>A major problem most machine learning systems face is retraining because in order for the model to stay up to<br>
date, you should constantly update the model weights based on the new data.</li>
</ul>
<p><a id="orgdc36e10"></a></p>
<h2 id="pipeline">Pipeline</h2>
<ul>
<li>A <code>database (DB)</code> to save the requests, the responses and all relative data</li>
<li>A <code>message queue (MQ)</code> to send them in the database in an asynchronous manner (to keep the system reliable and<br>
available)</li>
<li><code>Data jobs</code> to preprocess and transform the data in the correct format so that can be used from our model</li>
<li><code>Retrainer</code> instances that execute the actual training based on the saved data</li>
<li>After retraining the model, the new version will gradually replace all the UNet instances. That way we build<br>
deep learning <code>models versioning</code> functionality, where we always update the model with the newest version.</li>
<li>Finally, we need some sort of <code>monitoring</code> and <code>logging</code> to have a complete image of what’s happening in our system,<br>
to catch errors quickly and to discover bottlenecks easily.</li>
</ul>
<p><strong>NOTE</strong> Course suggestion: <a href="https://www.coursera.org/specializations/tensorflow-advanced-techniques?ranMID=40328&amp;ranEAID=r24KwW5qbBo&amp;ranSiteID=r24KwW5qbBo-SCC7WTJnpybOEZh8z5UxXQ&amp;siteID=r24KwW5qbBo-SCC7WTJnpybOEZh8z5UxXQ&amp;utm_content=10&amp;utm_medium=partners&amp;utm_source=linkshare&amp;utm_campaign=r24KwW5qbBo">TensorFlow: Advanced Techniques Specialization</a></p>
<p><a id="orge0dcd01"></a></p>
<h2 id="references">References</h2>
<ul>
<li><a href="https://www.nobledesktop.com/blog/what-is-git-and-why-should-you-use-it">Git and why should it be used?</a></li>
<li><a href="https://realpython.com/python-windows-machine-learning-setup/">Setting up python</a></li>
<li><a href="https://www.freecodecamp.org/news/how-to-configure-your-macos-terminal-with-zsh-like-a-pro-c0ab3f3c1156/">macOS terminal with zsh</a></li>
<li><a href="https://github.com/donnemartin/system-design-primer">System Design Primer</a></li>
</ul>
<p><a id="org487f124"></a></p>
<h1 id="doneprojectstructureofdeeplearningproject">DONE <a href="https://theaisummer.com/best-practices-deep-learning-code/">Project structure of Deep Learning project</a></h1>
<p>Organized, modularized and extensible python code is the goal.<br>
Machine learning code is ordinary software and should always be treated as one.</p>
<ul>
<li>as ordinary code it has a project structure, a documentation and design principles such as<br>
object-oriented programming.</li>
</ul>
<p><a id="orgc9e1d24"></a></p>
<h2 id="projectstructure">Project Structure</h2>
<p>The project <a href="https://github.com/The-AI-Summer/Deep-Learning-In-Production/tree/master/1.%20From%20a%20notebook%20to%20serving%20millions%20of%20users">structure</a> to follow for a deep learning project</p>
<p>A basic tree structure looks like this:</p>
<pre><code>├── __init__.py
├── configs
│   └── __init__.py
├── dataloader
│   └── __init__.py
├── evaluation
│   └── __init__.py
├── executor
│   └── __init__.py
├── main.py
├── model
│   └── __init__.py
├── notebooks
│   └── segmentation.ipynb
├── ops
│   └── __init__.py
└── utils
    └── __init__.py
</code></pre>
<p><strong>Module and a Package</strong><br>
Module:<br>
A module is simply a file containing Python code.</p>
<p>Package:<br>
A package, however, is like a directory that holds sub-packages and modules. In order for a package to be importable,<br>
it should contain a <code>init.py</code> file ( even if it’s empty).</p>
<p>In the following example we have 8 different packages:</p>
<ol>
<li>configs</li>
</ol>
<p>In configs we define every single thing that can be configurable and can be changed in the future. Good examples are<br>
training hyperparameters, folder paths, the model architecture, metrics, flags.</p>
<p>example:</p>
<pre><code class="language-python">CFG = {
   &quot;data&quot;: {
       &quot;path&quot;: &quot;oxford_iiit_pet:3.*.*&quot;,
       &quot;image_size&quot;: 128,
       &quot;load_with_info&quot;: True
   },
   &quot;train&quot;: {
       &quot;batch_size&quot;: 64,
       &quot;buffer_size&quot;: 1000,
       &quot;epoches&quot;: 20,
       &quot;val_subsplits&quot;: 5,
       &quot;optimizer&quot;: {
           &quot;type&quot;: &quot;adam&quot;
       },
       &quot;metrics&quot;: [&quot;accuracy&quot;]
   },
   &quot;model&quot;: {
       &quot;input&quot;: [128, 128, 3],
       &quot;up_stack&quot;: {
           &quot;layer_1&quot;: 512,
           &quot;layer_2&quot;: 256,
           &quot;layer_3&quot;: 128,
           &quot;layer_4&quot;: 64,
           &quot;kernels&quot;: 3
       },
       &quot;output&quot;: 3
   }
}
</code></pre>
<ol>
<li>
<p>dataloader<br>
It is quite self-explanatory. All the data loading and data preprocessing classes and functions live here.</p>
</li>
<li>
<p>evaluation<br>
It is a collection of code that aims to evaluate the performance and accuracy of our model.</p>
</li>
<li>
<p>executor<br>
we usually have all the functions and scripts that train the model or use it to predict something in different<br>
environments. And by different environments I mean: executors for GPUs, executors for distributed systems. This<br>
package is our connection with the outer world and it’s what our “main.py” will use.</p>
</li>
<li>
<p>model<br>
It contains the actual deep learning code (we talk about tensorflow, pytorch etc)</p>
</li>
<li>
<p>notebooks<br>
It include all of our jupyter/colab notebooks in one place.</p>
</li>
<li>
<p>ops<br>
This one is not always needed, as it includes operations not related with machine learning such as algebraic transformations,<br>
image manipulation techniques or maybe graph operations.</p>
</li>
<li>
<p>utils<br>
utilities functions that are used in more than one places and everything that don’t fall in on the above come here.</p>
</li>
</ol>
<p><a id="org52a11dd"></a></p>
<h2 id="objectorientedprogramminginpython">Object Oriented Programming in python</h2>
<p>If we are to code in OOP the UNet model would look like this:</p>
<p>To understand the &lt; Underscores in Python&gt;, read it and complete it.</p>
<pre><code class="language-python">class UNet():
    def __init__(self, config):
        self.base_model = tf.keras.applications.MobileNetV2( input_shape=self.config.model.input, include_top=False)
        self.batch_size = self.config.train.batch_size
        . . .

    def load_data(self):
        &quot;&quot;&quot;Loads and Preprocess data &quot;&quot;&quot;
        self.dataset, self.info = DataLoader().load_data(self.config.data)
        self._preprocess_data()

    def _preprocess_data(self):
        . . .

    def _set_training_parameters(self):
        . . .

    def _normalize(self, input_image, input_mask):
       . . .

    def _load_image_train(self, datapoint):
       . . .

    def _load_image_test(self, datapoint):
        . . .

    def build(self):
        &quot;&quot;&quot; Builds the Keras model based &quot;&quot;&quot;
        layer_names = [
            'block_1_expand_relu',  # 64x64
            'block_3_expand_relu',  # 32x32
            'block_6_expand_relu',  # 16x16
            'block_13_expand_relu',  # 8x8
            'block_16_project',  # 4x4
        ]
        layers = [self.base_model.get_layer(name).output for name in layer_names]

        . . .

        self.model = tf.keras.Model(inputs=inputs, outputs=x)

    def train(self):
        . . .

    def evaluate(self):
        . . .
</code></pre>
<p><a id="org2372f66"></a></p>
<h3 id="abstraction">Abstraction</h3>
<p>Using abstraction we can declare the desired <code>functionalities</code> without dealing with how they are going to be implemented.</p>
<pre><code class="language-python">class BaseModel(ABC):
    &quot;&quot;&quot;Abstract Model class that is inherited to all models&quot;&quot;&quot;
    def __init__(self, cfg):
        self.config = Config.from_json(cfg)

    @abstractmethod
    def load_data(self):
        pass

    @abstractmethod
    def build(self):
        pass

    @abstractmethod
    def train(self):
        pass

    @abstractmethod
    def evaluate(self):
        pass
</code></pre>
<ul>
<li>
<p>the functions have no body.</p>
</li>
<li>
<p>every functionality you are gonna need, declare it as an abstract function or class and you are done.</p>
</li>
<li>
<p>used by other classes that will “extend” our abstract class. This is called <code>inheritance</code>.</p>
</li>
<li>
<p>The base class will be inherited in the “child” class and it will immediately define the structure of the child class.</p>
<p>class UNet(BaseModel):<br>
def <strong>init</strong>(self, config):<br>
super().<strong>init</strong>(config)<br>
self.base_model = tf.keras.applications.MobileNetV2(input_shape=self.config.model.input, include_top=False)</p>
<pre><code class="language-python">   . . .

def load_data(self):
    self.dataset, self.info = DataLoader().load_data(self.config.data )
    self._preprocess_data()

    . . .

def build(self):

    . . .

    self.model = tf.keras.Model(inputs=inputs, outputs=x)

def train(self):
    self.model.compile(optimizer=self.config.train.optimizer.type,
                       loss=tf.keras.losses.SparseCategoricalCrossentropy(from_logits=True),
                       metrics=self.config.train.metrics)

    model_history = self.model.fit(self.train_dataset, epochs=self.epoches,
                                   steps_per_epoch=self.steps_per_epoch,
                                   validation_steps=self.validation_steps,
                                   validation_data=self.test_dataset)

    return model_history.history['loss'], model_history.history['val_loss']

def evaluate(self):
    predictions = []
    for image, mask in self.dataset.take(1):
        predictions.append(self.model.predict(image))

    return predictions
</code></pre>
</li>
</ul>
<p><a id="org56aa7e3"></a></p>
<h3 id="staticandclassmethods">Static and class methods</h3>
<ul>
<li>Makes our code less error-prone.</li>
<li>The <code>class</code> is like the blueprint when creating objects and objects are the instances of a class.</li>
<li><code>Class methods</code> take as arguments the class and they are usually used as constructors for creating new instances of the class.</li>
<li><code>Static methods</code> are methods that refer only to the class, not its instances, and they will not modify the class state.</li>
<li>Static methods are mostly used for utils functions that aren’t gonna change.</li>
</ul>
<p>The classmethod “from<sub>json</sub>” returns an actual instance of the Config class created from a json file by just calling “Config.from<sub>json</sub>( json<sub>file</sub> )”</p>
<pre><code class="language-python">class Config:
    &quot;&quot;&quot;Config class which contains data, train and model hyperparameters&quot;&quot;&quot;

    def __init__(self, data, train, model):
        self.data = data
        self.train = train
        self.model = model

    @classmethod
    def from_json(cls, cfg):
        &quot;&quot;&quot;Creates config from json&quot;&quot;&quot;
        params = json.loads(json.dumps(cfg), object_hook=HelperObject)
        return cls(params.data, params.train, params.model)
</code></pre>
<p><strong>Static methods</strong> on the other hand are methods that are called on the actual object and not some instance of it.</p>
<ul>
<li>
<p>A perfect example in our case is the DataLoader class, where we simply want to load the data from a url.</p>
</li>
<li>
<p>Is there a reason to have a new DataLoader instance?</p>
<ul>
<li>Not really, because everything is stable in this functionality and nothing will ever change.</li>
</ul>
<p>class DataLoader:<br>
&quot;&quot;&quot;Data Loader class&quot;&quot;&quot;</p>
<pre><code class="language-python">@staticmethod
def load_data(data_config):
    &quot;&quot;&quot;Loads dataset from path&quot;&quot;&quot;
    return tfds.load(data_config.path, with_info=data_config.load_with_info)
</code></pre>
</li>
</ul>
<p><a id="org786b4f5"></a></p>
<h2 id="typechecking">Type checking</h2>
<p>Type checking is the process of verifying and enforcing the constraints of types.</p>
<pre><code class="language-python">def ai_summer_func(x:int) -&gt; int:
    return x+5
</code></pre>
<p><a id="org15dfd66"></a></p>
<h2 id="documentation">Documentation</h2>
<p>Documenting our code is the single most important thing in this list and the thing that most of us are guilty of not doing.</p>
<p><strong>BAD</strong></p>
<pre><code class="language-python">def n(self, ii, im):

    ii = tf.cast(ii, tf.float32) / 255.0
    im -= 1
    return ii, im
</code></pre>
<p><strong>GOOD</strong></p>
<pre><code class="language-python">def _normalize(self, input_image, input_mask):
       &quot;&quot;&quot; Normalise input image
       Args:
           input_image (tf.image): The input image
           input_mask (int): The image mask

       Returns:
           input_image (tf.image): The normalized input image
           input_mask (int): The new image mask
       &quot;&quot;&quot;
       input_image = tf.cast(input_image, tf.float32) / 255.0
       input_mask -= 1
       return input_image, input_mask
</code></pre>
<p>The comments you can see above are called <code>docstrings</code> and are python’s way to document the responsibility of a piece of code.</p>
<p>The <code>=first line=</code> always indicates what the code does and it is the only real essential part (I personally try to always include this<br>
in my code). The other parts explain what arguments the function accepts (with types and descriptions) and what it returns.<br>
These can sometimes be ignored if they don’t provide much value. This is based on <code>Google's style guide</code>.</p>
<p><a id="orgc95c330"></a></p>
<h2 id="references">References</h2>
<ul>
<li><a href="https://www.learnpython.org/en/Modules_and_Packages">Modules and packages</a></li>
<li><a href="https://realpython.com/python3-object-oriented-programming/">OOP in python</a></li>
<li><a href="https://stackoverflow.com/questions/136097/difference-between-staticmethod-and-classmethod">Static method and classmethod</a></li>
<li><a href="https://www.journaldev.com/33542/python-type-checking">Python type checking</a></li>
<li><a href="https://www.datacamp.com/community/tutorials/docstrings-python">Docstrings in Python</a></li>
</ul>
<p><a id="org1dc87e3"></a></p>
<h1 id="doneunittesting">DONE Unit Testing</h1>
<p>source : <a href="https://theaisummer.com/unit-test-deep-learning/">https://theaisummer.com/unit-test-deep-learning/</a></p>
<p><a id="org5c09b22"></a></p>
<h2 id="whyunittesting">Why unit testing?</h2>
<p>When developing a neural network, most of us don’t care about catching all possible exceptions, finding all corner cases,<br>
or debugging every single function.</p>
<p>Unit tests are tremendously useful because they:</p>
<ul>
<li>Find software bugs early</li>
<li>Debug our code</li>
<li>Ensure that the code does what is supposed to do</li>
<li>Simplify the refactoring process</li>
<li>Speed up the integration process</li>
<li>Act as documentation</li>
</ul>
<p><a id="orgbb414ab"></a></p>
<h2 id="basicsofunittesting">Basics of unit testing</h2>
<p>Unit testing is just a <code>=function calling another function=</code> (or a unit) and checking if the values returned<br>
match the expected output.</p>
<p>We have this simple function that normalized an image by dividing all the pixels by 255.</p>
<pre><code class="language-python">def _normalize(self, input_image, input_mask):
    &quot;&quot;&quot; Normalise input image
    Args:
        input_image (tf.image): The input image
        input_mask (int): The image mask

    Returns:
        input_image (tf.image): The normalized input image
        input_mask (int): The new image mask
    &quot;&quot;&quot;
    input_image = tf.cast(input_image, tf.float32) / 255.0
    input_mask -= 1
    return input_image, input_mask
</code></pre>
<p>To make sure that it does exactly what it is supposed to do, we can write <code>=another function that uses the “_normalize”=</code><br>
and check its result. It will look something like this.</p>
<pre><code class="language-python">def test_normalize(self):
    input_image = np.array([[1., 1.], [1., 1.]])
    input_mask = 1
    expected_image = np.array([[0.00392157, 0.00392157], [0.00392157, 0.00392157]])

    result = self.unet._normalize(input_image, input_mask)
    self.assertEquals(expected_image, result[0])
</code></pre>
<p>The <code>“test_normalize”</code> function creates a fake input image, calls the function with that image as an argument, and then makes<br>
sure that the result is equal to the expected image.</p>
<p>The <code>“assertEquals”</code> is a special function, coming from the unittest package in python, it asserts that the two values are equal.</p>
<p><a id="org54688cb"></a></p>
<h2 id="unittestsinpython">Unit tests in Python</h2>
<p>The main test framework/runner that comes into Python’s standard library is <a href="https://docs.python.org/3/library/unittest.html">unittest</a>.</p>
<p>Unittest is pretty straightforward to use and it has only two requirements:</p>
<ul>
<li>
<p>to put your tests into a class</p>
</li>
<li>
<p>use its special assert functions</p>
<p>import unittest</p>
<p>class UnetTest(unittest.TestCase):</p>
<pre><code class="language-python">def test_normalize(self):
    . . .
</code></pre>
<p>if <strong>name</strong> == '<strong>main</strong>':<br>
unittest.main()</p>
</li>
</ul>
<p><strong>Important points to notice here</strong></p>
<ul>
<li>In general, test functions are named with <code>“test_”</code> as a prefix followed by the name of the function they test.</li>
<li>To run unit tests, we call the “unittest.main()” function which discovers all tests within the module, runs them<br>
and prints their output.</li>
<li>Another two powerful frameworks are <a href="https://docs.pytest.org/en/stable/">pytest</a> and <a href="https://nose.readthedocs.io/en/latest/">nose</a>, which are pretty much governed by the same principles.</li>
</ul>
<p><strong>Tests in Tensorflow</strong><br>
Since we use Tensorflow to program our model we can take advantage of <a href="https://www.tensorflow.org/api_docs/python/tf/test">tf.test</a></p>
<pre><code class="language-python">import tensorflow as tf
</code></pre>
<p>​</p>
<pre><code class="language-python">class UnetTest(tf.test.TestCase):

    def setUp(self):
        super(UnetTest, self).setUp()
        . . .

    def tearDown(self):
        pass

    def test_normalize(self):
        . . .
</code></pre>
<p>​</p>
<pre><code class="language-python">if __name__ == '__main__':
    tf.test.main()
</code></pre>
<p><a id="orgf42bfcf"></a></p>
<h2 id="mocking">Mocking</h2>
<p><code>Mocking</code> makes it very easy to replace complex logic or heavy dependencies when testing code using <code>=dummy objects=</code>.<br>
By <code>dummy objects</code>, we refer to simple, easy to code objects that have the same structure with our real objects but contain<br>
<code>fake</code> or <code>useless</code> data.</p>
<p>In our case a dummy object might be a 2d tensor with all ones which mimics an actual image (just like the “input<sub>image</sub> in<br>
the first code snippet).</p>
<p>Mocking also helps us control the code’s behavior and simulate expensive calls. Let’s look at an example using once again our UNet.</p>
<pre><code class="language-python">def load_data(self):
        &quot;&quot;&quot; Loads and Preprocess data &quot;&quot;&quot;
        self.dataset, self.info = DataLoader().load_data(self.config.data)
        self._preprocess_data()

def _preprocess_data(self):
    &quot;&quot;&quot; Splits into training and test and set training parameters&quot;&quot;&quot;
    train = self.dataset['train'].map(self._load_image_train, num_parallel_calls=tf.data.experimental.AUTOTUNE)
    test = self.dataset['test'].map(self._load_image_test)

    self.train_dataset = train.cache().shuffle(self.buffer_size).batch(self.batch_size).repeat()
    self.train_dataset = self.train_dataset.prefetch(buffer_size=tf.data.experimental.AUTOTUNE)
    self.test_dataset = test.batch(self.batch_size)

def _load_image_train(self, datapoint):
    &quot;&quot;&quot; Loads and preprocess  a single training image &quot;&quot;&quot;
    input_image = tf.image.resize(datapoint['image'], (self.image_size, self.image_size))
    input_mask = tf.image.resize(datapoint['segmentation_mask'], (self.image_size, self.image_size))

    if tf.random.uniform(()) &gt; 0.5:
        input_image = tf.image.flip_left_right(input_image)
        input_mask = tf.image.flip_left_right(input_mask)

    input_image, input_mask = self._normalize(input_image, input_mask)

    return input_image, input_mask

def _load_image_test(self, datapoint):
    &quot;&quot;&quot; Loads and preprocess a single test image&quot;&quot;&quot;

    input_image = tf.image.resize(datapoint['image'], (self.image_size, self.image_size))
    input_mask = tf.image.resize(datapoint['segmentation_mask'], (self.image_size, self.image_size))

    input_image, input_mask = self._normalize(input_image, input_mask)

    return input_image, input_mask
</code></pre>
<p>What this code does is splitting the data, some shuffling, some resizing, and batching.<br>
So we want to test this code.<br>
Everything is nice and well except that freaking loading function.</p>
<pre><code class="language-python">self.dataset, self.info = DataLoader().load_data(self.config.data)
</code></pre>
<p>Are we supposed to load the entire data every time we run a single unit test?<br>
Nah,</p>
<p>Therefore, we could <code>mock</code> that function to return a dummy dataset instead of calling the real one.<br>
We can do that with unittest’s <a href="https://docs.python.org/3/library/unittest.mock.html">mock object package</a><br>
It provides a mock class “Mock()” to create a mock object directly and a “patch()” decorator that replaces<br>
an imported module within the module we test with a mock object.</p>
<blockquote>
<p>For those who aren’t familiar, the decorator is simply a function that wraps another function to extend its<br>
functionality. Once we declare the wrapper function we can annotate other functions to enhance them. See the<br>
@patch below? That’s a decorator that wraps the “test<sub>load</sub><sub>data</sub>” with the “patch” function. For more information<br>
follow the link at the end of the post.</p>
</blockquote>
<p>By using the “patch()” decorator we get this:</p>
<pre><code class="language-python">@patch('model.unet.DataLoader.load_data')
def test_load_data(self, mock_data_loader):
    mock_data_loader.side_effect = dummy_load_data
    shape = tf.TensorShape([None, self.unet.image_size, self.unet.image_size, 3])

    self.unet.load_data()
    mock_data_loader.assert_called()

    self.assertItemsEqual(self.unet.train_dataset.element_spec[0].shape, shape)
    self.assertItemsEqual(self.unet.test_dataset.element_spec[0].shape, shape)
</code></pre>
<p><a id="orgb4cdacd"></a></p>
<h2 id="testcoverage">Test coverage</h2>
<p>By coverage, we mean how much of our code is actually tested by unit tests.<br>
Coverage is an invaluable metric that can help us write better unit tests, discover which areas<br>
our tests don’t exercise, find new test cases, and ensure the quality of our tests.</p>
<pre><code class="language-python">coverage run -m unittest /path_to_folder/tests/unet_test.py

coverage report -m /path_to_folder/tests/unet_test.py

output--

Name                          Stmts   Miss    Cover   Missing
-------------------------------------------------------------
model/tests/unet_test.py       35      1       97%      52
</code></pre>
<p>This says that we cover 97% of our code. There are 35 statements in total and we missed just 1 of them.<br>
The missing info tells us which lines of code still need coverage.</p>
<p><a id="orgc0eb724"></a></p>
<h2 id="testexamples">Test examples</h2>
<p>Explore some of the different deep learning scenarios and parts of the codebase when unit testing can be incredibly useful.</p>
<p><strong>DATA</strong></p>
<ul>
<li>Ensure that our data has the right format (yes I put it again here for completion)</li>
<li>Ensure that the training labels are correct</li>
<li>Test our complex processing steps such as image manipulation</li>
<li>Assert data completion, quality, and errors</li>
<li>Test the distribution of the features</li>
</ul>
<p><strong>TRAINING</strong></p>
<ul>
<li>Run a training step and compare the weight before and after to ensure that they are updated</li>
<li>Check that our loss function can be actually used on our data</li>
</ul>
<p><strong>EVALUATION</strong></p>
<ul>
<li>Having tests to ensure that your metrics ( e.g accuracy, precision, and recall ) are above a threshold when iterating</li>
</ul>
<p>over different architectures</p>
<ul>
<li>You can run speed/benchmark tests on training to catch possible overfitting</li>
<li>Of course, cross-validation can be in the form of a unit test</li>
</ul>
<p><strong>MODEL ARCHITECTURE</strong></p>
<ul>
<li>The model’s layers are actually stacking</li>
<li>The model’s output has the correct shape</li>
</ul>
<p>example:</p>
<pre><code class="language-python">def test_ouput_size(self):
    shape = (1, self.unet.image_size, self.unet.image_size, 3)
    image = tf.ones(shape)
    self.unet.build()
    self.assertEqual(self.unet.model.predict(image).shape, shape)
</code></pre>
<p>Define the expected shape, construct a dummy input, build the model, and run a prediction is all it takes.<br>
Sometimes a few lines of code can save us from a lot of trouble.</p>
<p><a id="org0e0c7fa"></a></p>
<h2 id="integration">Integration</h2>
<p>These kinds of tests are very powerful tools and aim to test how well our system integrates with other systems.<br>
We want to be certain that the model returns what the user/client expects in the form that he expects it.</p>
<p><a id="org400fe94"></a></p>
<h2 id="conclusion">Conclusion</h2>
<ul>
<li>Unit tests are indeed an invaluable tool in our arsenal, especially when building complex deep learning models.</li>
<li>My laziness has come and bitten me more times than I can count, so I decided to always write them from now on.</li>
<li>But again, we always need to find a balance.</li>
</ul>
<p><a id="orgd3f9aaf"></a></p>
<h2 id="references">References</h2>
<ul>
<li><a href="https://www.programiz.com/python-programming/decorator">Python decorators</a></li>
<li><a href="https://en.wikipedia.org/wiki/Code_coverage">Code coverage</a></li>
<li><a href="https://www.toptal.com/python/an-introduction-to-mocking-in-python">Mocking</a></li>
<li><a href="https://realpython.com/python-testing/">Testing in Python</a></li>
</ul>
<p><a id="orgbb96761"></a></p>
<h1 id="inprogresslogginganddebugging">IN-PROGRESS Logging and Debugging</h1>
<p>source :: <a href="https://theaisummer.com/logging-debugging/">https://theaisummer.com/logging-debugging/</a></p>
<p><a id="orge9c000a"></a></p>
<h2 id="debugdeeplearning">Debug deep learning</h2>
<p><a id="orgeb14bd2"></a></p>
<h2 id="logging">Logging</h2>
<p><a id="org3e41322"></a></p>
<h2 id="tensorflowlogginganddebugging">Tensorflow logging and debugging</h2>
<!--kg-card-end: markdown-->]]></content:encoded></item><item><title><![CDATA[(Untitled)]]></title><description><![CDATA[<p><br></p><p># Table of Contents</p><p><br></p><p>1.  [laptop setup and system design for deep learning](#org94aa6ec)</p><p>1.  [Software design principles](#org441c7ea)</p><p>2.  [Points to ponder](#org592b6d3)</p><p>3.  [Pipeline](#orgdc36e10)</p><p>4.  [References](#orge0dcd01)</p><p>2.  [Project structure of Deep Learning project](#org487f124)</p><p>1.  [Project Structure](#orgc9e1d24)</p><p>2.  [Object Oriented Programming in python](#org52a11dd)</p><p>1.  [Abstraction]</p>]]></description><link>nezubn.com/untitled/</link><guid isPermaLink="false">6007b20e56c39f44086f9afa</guid><dc:creator><![CDATA[Ankit Sharma]]></dc:creator><pubDate>Wed, 20 Jan 2021 04:32:06 GMT</pubDate><content:encoded><![CDATA[<p><br></p><p># Table of Contents</p><p><br></p><p>1.  [laptop setup and system design for deep learning](#org94aa6ec)</p><p>1.  [Software design principles](#org441c7ea)</p><p>2.  [Points to ponder](#org592b6d3)</p><p>3.  [Pipeline](#orgdc36e10)</p><p>4.  [References](#orge0dcd01)</p><p>2.  [Project structure of Deep Learning project](#org487f124)</p><p>1.  [Project Structure](#orgc9e1d24)</p><p>2.  [Object Oriented Programming in python](#org52a11dd)</p><p>1.  [Abstraction](#org2372f66)</p><p>2.  [Static and class methods](#org56aa7e3)</p><p>3.  [Type checking](#org786b4f5)</p><p>4.  [Documentation](#org15dfd66)</p><p>5.  [References](#orgc95c330)</p><p>3.  [Unit Testing](#org1dc87e3)</p><p>1.  [Why unit testing?](#org5c09b22)</p><p>2.  [Basics of unit testing](#orgbb414ab)</p><p>3.  [Unit tests in Python](#org54688cb)</p><p>4.  [Mocking](#orgf42bfcf)</p><p>5.  [Test coverage](#orgb4cdacd)</p><p>6.  [Test examples](#orgc0eb724)</p><p>7.  [Integration](#org0e0c7fa)</p><p>8.  [Conclusion](#org400fe94)</p><p>9.  [References](#orgd3f9aaf)</p><p>4.  [Logging and Debugging](#orgbb96761)</p><p>1.  [Debug deep learning](#orge9c000a)</p><p>2.  [Logging](#orgeb14bd2)</p><p>3.  [Tensorflow logging and debugging](#org3e41322)</p><p><br></p><p><br></p><p>&lt;a id="org94aa6ec"&gt;&lt;/a&gt;</p><p><br></p><p># DONE [laptop setup and system design for deep learning](https://theaisummer.com/deep-learning-production/)</p><p><br></p><p>Suggested Tools: Git, Github, PyCharm, terminal, slack, zenhub, conda, zoom</p><p><br></p><p>What distinguishes a good engineer from a great engineer?</p><p><br></p><p>-   His ability to plan and design the system before even touching the keyboard. Carefully thinking about the</p><p>architecture, making informed decisions, discussing it with other engineers, and documenting things can save</p><p>a lot of time beforehand. A whole lot of time. Trust me on this. I’m guilty of this more times than I’m proud</p><p>to admit. That’s why, this time we will do it the right way. Thus, it turns out it won’t be such a short</p><p>parenthesis!</p><p><br></p><p>Questions in our mind?</p><p><br></p><p>-   What will happen if more than 1 users ask the model at the same time?</p><p>-   What will happen if for some reason the model crashes?</p><p>-   If it contains a bug we haven’t previously seen?</p><p>-   If the model is too slow and the user has to wait for too long to get a response?</p><p>-   And how can we update our model?</p><p>-   Can we retrain it on new data?</p><p>-   Can we get some feedback if the model performs well?</p><p>-   Can we see how fast or slow the model is?</p><p>-   How much memory does it use?</p><p>-   Where do we store the model weights?</p><p>-   What about the training data?</p><p>-   What if the user sends a malicious request instead of a proper image?</p><p><br></p><p><br></p><p>&lt;a id="org441c7ea"&gt;&lt;/a&gt;</p><p><br></p><p>## Software design principles</p><p><br></p><p>1.  Separation of concerns</p><p>The system should be modularized into different components with each component being a separate maintainable,</p><p>reusable and extensible entity.</p><p>2.  Scalibility</p><p>The system needs to be able to scale as the traffic increases</p><p>3.  Reliability</p><p>The system should continue to be functional even if there is software of hardw   are failure</p><p>4.  Availability</p><p>The system needs to continue operating at all times</p><p>5.  Simplicity</p><p>The system has to be as simple and intuitive as possible</p><p><br></p><p><br></p><p>&lt;a id="org592b6d3"&gt;&lt;/a&gt;</p><p><br></p><p>## Points to ponder</p><p><br></p><p>-   Each user sends a request to our backend. To be able to handle all the simultaneous requests, first we need</p><p>a `Load Balancer` (LB) to handle the traffic and route the requests to the proper server instance.</p><p>-   We maintain more than one instance of the model in order to achieve both scalability and availability. Note</p><p>that these might be physical instances, virtual machines or docker containers and they are organized by a</p><p>distributed system orchestrator (such as Kubernetes)</p><p>-   As you can probably see, each instance may have more than one threads or parallel processes. We need that to</p><p>make sure that the model is fast enough to return a response in real time.</p><p>-   A major problem most machine learning systems face is retraining because in order for the model to stay up to</p><p>date, you should constantly update the model weights based on the new data.</p><p><br></p><p><br></p><p>&lt;a id="orgdc36e10"&gt;&lt;/a&gt;</p><p><br></p><p>## Pipeline</p><p><br></p><p>-   A `database (DB)` to save the requests, the responses and all relative data</p><p>-   A `message queue (MQ)` to send them in the database in an asynchronous manner (to keep the system reliable and</p><p>available)</p><p>-   `Data jobs` to preprocess and transform the data in the correct format so that can be used from our model</p><p>-   `Retrainer` instances that execute the actual training based on the saved data</p><p>-   After retraining the model, the new version will gradually replace all the UNet instances. That way we build</p><p>deep learning `models versioning` functionality, where we always update the model with the newest version.</p><p>-   Finally, we need some sort of `monitoring` and `logging` to have a complete image of what’s happening in our system,</p><p>to catch errors quickly and to discover bottlenecks easily.</p><p><br></p><p>**<strong>NOTE</strong>** Course suggestion: [TensorFlow: Advanced Techniques Specialization](https://www.coursera.org/specializations/tensorflow-advanced-techniques?ranMID=40328&amp;ranEAID=r24KwW5qbBo&amp;ranSiteID=r24KwW5qbBo-SCC7WTJnpybOEZh8z5UxXQ&amp;siteID=r24KwW5qbBo-SCC7WTJnpybOEZh8z5UxXQ&amp;utm_content=10&amp;utm_medium=partners&amp;utm_source=linkshare&amp;utm_campaign=r24KwW5qbBo)</p><p><br></p><p><br></p><p>&lt;a id="orge0dcd01"&gt;&lt;/a&gt;</p><p><br></p><p>## References</p><p><br></p><p>-   [Git and why should it be used?](https://www.nobledesktop.com/blog/what-is-git-and-why-should-you-use-it)</p><p>-   [Setting up python](https://realpython.com/python-windows-machine-learning-setup/)</p><p>-   [macOS terminal with zsh](https://www.freecodecamp.org/news/how-to-configure-your-macos-terminal-with-zsh-like-a-pro-c0ab3f3c1156/)</p><p>-   [System Design Primer](https://github.com/donnemartin/system-design-primer)</p><p><br></p><p><br></p><p>&lt;a id="org487f124"&gt;&lt;/a&gt;</p><p><br></p><p># DONE [Project structure of Deep Learning project](https://theaisummer.com/best-practices-deep-learning-code/)</p><p><br></p><p>Organized, modularized and extensible python code is the goal.</p><p>Machine learning code is ordinary software and should always be treated as one.</p><p><br></p><p>-   as ordinary code it has a project structure, a documentation and design principles such as</p><p>object-oriented programming.</p><p><br></p><p><br></p><p>&lt;a id="orgc9e1d24"&gt;&lt;/a&gt;</p><p><br></p><p>## Project Structure</p><p><br></p><p>The project [structure](https://github.com/The-AI-Summer/Deep-Learning-In-Production/tree/master/1.%20From%20a%20notebook%20to%20serving%20millions%20of%20users) to follow for a deep learning project</p><p><br></p><p>A basic tree structure looks like this:</p><p><br></p><p>├── __init__.py</p><p>├── configs</p><p>│   └── __init__.py</p><p>├── dataloader</p><p>│   └── __init__.py</p><p>├── evaluation</p><p>│   └── __init__.py</p><p>├── executor</p><p>│   └── __init__.py</p><p>├── main.py</p><p>├── model</p><p>│   └── __init__.py</p><p>├── notebooks</p><p>│   └── segmentation.ipynb</p><p>├── ops</p><p>│   └── __init__.py</p><p>└── utils</p><p>└── __init__.py</p><p><br></p><p>**<strong>Module and a Package</strong>**</p><p>Module:</p><p>A module is simply a file containing Python code.</p><p><br></p><p>Package:</p><p>A package, however, is like a directory that holds sub-packages and modules. In order for a package to be importable,</p><p>it should contain a `init.py` file ( even if it’s empty).</p><p><br></p><p>In the following example we have 8 different packages:</p><p><br></p><p>1.  configs</p><p><br></p><p>In configs we define every single thing that can be configurable and can be changed in the future. Good examples are</p><p>training hyperparameters, folder paths, the model architecture, metrics, flags.</p><p><br></p><p>example:</p><p><br></p><p>CFG = {</p><p>"data": {</p><p>"path": "oxford_iiit_pet:3.*.*",</p><p>"image_size": 128,</p><p>"load_with_info": True</p><p>},</p><p>"train": {</p><p>"batch_size": 64,</p><p>"buffer_size": 1000,</p><p>"epoches": 20,</p><p>"val_subsplits": 5,</p><p>"optimizer": {</p><p>"type": "adam"</p><p>},</p><p>"metrics": ["accuracy"]</p><p>},</p><p>"model": {</p><p>"input": [128, 128, 3],</p><p>"up_stack": {</p><p>"layer_1": 512,</p><p>"layer_2": 256,</p><p>"layer_3": 128,</p><p>"layer_4": 64,</p><p>"kernels": 3</p><p>},</p><p>"output": 3</p><p>}</p><p>}</p><p><br></p><p>1.  dataloader</p><p>It is quite self-explanatory. All the data loading and data preprocessing classes and functions live here.</p><p><br></p><p>2.  evaluation</p><p>It is a collection of code that aims to evaluate the performance and accuracy of our model.</p><p><br></p><p>3.  executor</p><p>we usually have all the functions and scripts that train the model or use it to predict something in different</p><p>environments. And by different environments I mean: executors for GPUs, executors for distributed systems. This</p><p>package is our connection with the outer world and it’s what our “main.py” will use.</p><p><br></p><p>4.  model</p><p>It contains the actual deep learning code (we talk about tensorflow, pytorch etc)</p><p><br></p><p>5.  notebooks</p><p>It include all of our jupyter/colab notebooks in one place.</p><p><br></p><p>6.  ops</p><p>This one is not always needed, as it includes operations not related with machine learning such as algebraic transformations,</p><p>image manipulation techniques or maybe graph operations.</p><p><br></p><p>7.  utils</p><p>utilities functions that are used in more than one places and everything that don’t fall in on the above come here.</p><p><br></p><p><br></p><p>&lt;a id="org52a11dd"&gt;&lt;/a&gt;</p><p><br></p><p>## Object Oriented Programming in python</p><p><br></p><p>If we are to code in OOP the UNet model would look like this:</p><p><br></p><p>To understand the &lt; Underscores in Python&gt;, read it and complete it.</p><p><br></p><p>class UNet():</p><p>def __init__(self, config):</p><p>self.base_model = tf.keras.applications.MobileNetV2( input_shape=self.config.model.input, include_top=False)</p><p>self.batch_size = self.config.train.batch_size</p><p>. . .</p><p>def load_data(self):</p><p>"""Loads and Preprocess data """</p><p>self.dataset, self.info = DataLoader().load_data(self.config.data)</p><p>self._preprocess_data()</p><p>def _preprocess_data(self):</p><p>. . .</p><p>def _set_training_parameters(self):</p><p>. . .</p><p>def _normalize(self, input_image, input_mask):</p><p>. . .</p><p>def _load_image_train(self, datapoint):</p><p>. . .</p><p>def _load_image_test(self, datapoint):</p><p>. . .</p><p>def build(self):</p><p>""" Builds the Keras model based """</p><p>layer_names = [</p><p>'block_1_expand_relu',  # 64x64</p><p>'block_3_expand_relu',  # 32x32</p><p>'block_6_expand_relu',  # 16x16</p><p>'block_13_expand_relu',  # 8x8</p><p>'block_16_project',  # 4x4</p><p>]</p><p>layers = [self.base_model.get_layer(name).output for name in layer_names]</p><p>. . .</p><p>self.model = tf.keras.Model(inputs=inputs, outputs=x)</p><p>def train(self):</p><p>. . .</p><p>def evaluate(self):</p><p>. . .</p><p><br></p><p><br></p><p>&lt;a id="org2372f66"&gt;&lt;/a&gt;</p><p><br></p><p>### Abstraction</p><p><br></p><p>Using abstraction we can declare the desired `functionalities` without dealing with how they are going to be implemented.</p><p><br></p><p>class BaseModel(ABC):</p><p>"""Abstract Model class that is inherited to all models"""</p><p>def __init__(self, cfg):</p><p>self.config = Config.from_json(cfg)</p><p>@abstractmethod</p><p>def load_data(self):</p><p>pass</p><p>@abstractmethod</p><p>def build(self):</p><p>pass</p><p>@abstractmethod</p><p>def train(self):</p><p>pass</p><p>@abstractmethod</p><p>def evaluate(self):</p><p>pass</p><p><br></p><p>-   the functions have no body.</p><p>-   every functionality you are gonna need, declare it as an abstract function or class and you are done.</p><p>-   used by other classes that will “extend” our abstract class. This is called `inheritance`.</p><p>-   The base class will be inherited in the “child” class and it will immediately define the structure of the child class.</p><p><br></p><p>class UNet(BaseModel):</p><p>def __<strong>init</strong>__(self, config):</p><p>super().__<strong>init</strong>__(config)</p><p>self.base_model = tf.keras.applications.MobileNetV2(input_shape=self.config.model.input, include_top=False)</p><p>. . .</p><p>def load_data(self):</p><p>self.dataset, self.info = DataLoader().load_data(self.config.data )</p><p>self._preprocess_data()</p><p>. . .</p><p>def build(self):</p><p>. . .</p><p>self.model = tf.keras.Model(inputs=inputs, outputs=x)</p><p>def train(self):</p><p>self.model.compile(optimizer=self.config.train.optimizer.type,</p><p>loss=tf.keras.losses.SparseCategoricalCrossentropy(from_logits=True),</p><p>metrics=self.config.train.metrics)</p><p>model_history = self.model.fit(self.train_dataset, epochs=self.epoches,</p><p>steps_per_epoch=self.steps_per_epoch,</p><p>validation_steps=self.validation_steps,</p><p>validation_data=self.test_dataset)</p><p>return model_history.history['loss'], model_history.history['val_loss']</p><p>def evaluate(self):</p><p>predictions = []</p><p>for image, mask in self.dataset.take(1):</p><p>predictions.append(self.model.predict(image))</p><p>return predictions</p><p><br></p><p><br></p><p>&lt;a id="org56aa7e3"&gt;&lt;/a&gt;</p><p><br></p><p>### Static and class methods</p><p><br></p><p>-   Makes our code less error-prone.</p><p>-   The `class` is like the blueprint when creating objects and objects are the instances of a class.</p><p>-   `Class methods` take as arguments the class and they are usually used as constructors for creating new instances of the class.</p><p>-   `Static methods` are methods that refer only to the class, not its instances, and they will not modify the class state.</p><p>-   Static methods are mostly used for utils functions that aren’t gonna change.</p><p><br></p><p>The classmethod “from&lt;sub&gt;json&lt;/sub&gt;” returns an actual instance of the Config class created from a json file by just calling “Config.from&lt;sub&gt;json&lt;/sub&gt;( json&lt;sub&gt;file&lt;/sub&gt; )”</p><p><br></p><p>class Config:</p><p>"""Config class which contains data, train and model hyperparameters"""</p><p>def __init__(self, data, train, model):</p><p>self.data = data</p><p>self.train = train</p><p>self.model = model</p><p>@classmethod</p><p>def from_json(cls, cfg):</p><p>"""Creates config from json"""</p><p>params = json.loads(json.dumps(cfg), object_hook=HelperObject)</p><p>return cls(params.data, params.train, params.model)</p><p><br></p><p>**<strong>Static methods</strong>** on the other hand are methods that are called on the actual object and not some instance of it.</p><p><br></p><p>-   A perfect example in our case is the DataLoader class, where we simply want to load the data from a url.</p><p>-   Is there a reason to have a new DataLoader instance?</p><p>-   Not really, because everything is stable in this functionality and nothing will ever change.</p><p><br></p><p>class DataLoader:</p><p>"""Data Loader class"""</p><p>@staticmethod</p><p>def load_data(data_config):</p><p>"""Loads dataset from path"""</p><p>return tfds.load(data_config.path, with_info=data_config.load_with_info)</p><p><br></p><p><br></p><p>&lt;a id="org786b4f5"&gt;&lt;/a&gt;</p><p><br></p><p>## Type checking</p><p><br></p><p>Type checking is the process of verifying and enforcing the constraints of types.</p><p><br></p><p>def ai_summer_func(x:int) -&gt; int:</p><p>return x+5</p><p><br></p><p><br></p><p>&lt;a id="org15dfd66"&gt;&lt;/a&gt;</p><p><br></p><p>## Documentation</p><p><br></p><p>Documenting our code is the single most important thing in this list and the thing that most of us are guilty of not doing.</p><p><br></p><p>**<strong>BAD</strong>**</p><p><br></p><p>def n(self, ii, im):</p><p>ii = tf.cast(ii, tf.float32) / 255.0</p><p>im -= 1</p><p>return ii, im</p><p><br></p><p>**<strong>GOOD</strong>**</p><p><br></p><p>def _normalize(self, input_image, input_mask):</p><p>""" Normalise input image</p><p>Args:</p><p>input_image (tf.image): The input image</p><p>input_mask (int): The image mask</p><p>Returns:</p><p>input_image (tf.image): The normalized input image</p><p>input_mask (int): The new image mask</p><p>"""</p><p>input_image = tf.cast(input_image, tf.float32) / 255.0</p><p>input_mask -= 1</p><p>return input_image, input_mask</p><p><br></p><p>The comments you can see above are called `docstrings` and are python’s way to document the responsibility of a piece of code.</p><p><br></p><p>The `=first line=` always indicates what the code does and it is the only real essential part (I personally try to always include this</p><p>in my code). The other parts explain what arguments the function accepts (with types and descriptions) and what it returns.</p><p>These can sometimes be ignored if they don’t provide much value. This is based on `Google's style guide`.</p><p><br></p><p><br></p><p>&lt;a id="orgc95c330"&gt;&lt;/a&gt;</p><p><br></p><p>## References</p><p><br></p><p>-   [Modules and packages](https://www.learnpython.org/en/Modules_and_Packages)</p><p>-   [OOP in python](https://realpython.com/python3-object-oriented-programming/)</p><p>-   [Static method and classmethod](https://stackoverflow.com/questions/136097/difference-between-staticmethod-and-classmethod)</p><p>-   [Python type checking](https://www.journaldev.com/33542/python-type-checking)</p><p>-   [Docstrings in Python](https://www.datacamp.com/community/tutorials/docstrings-python)</p><p><br></p><p><br></p><p>&lt;a id="org1dc87e3"&gt;&lt;/a&gt;</p><p><br></p><p># DONE Unit Testing</p><p><br></p><p>source : &lt;https://theaisummer.com/unit-test-deep-learning/&gt;</p><p><br></p><p><br></p><p>&lt;a id="org5c09b22"&gt;&lt;/a&gt;</p><p><br></p><p>## Why unit testing?</p><p><br></p><p>When developing a neural network, most of us don’t care about catching all possible exceptions, finding all corner cases,</p><p>or debugging every single function.</p><p><br></p><p>Unit tests are tremendously useful because they:</p><p><br></p><p>-   Find software bugs early</p><p>-   Debug our code</p><p>-   Ensure that the code does what is supposed to do</p><p>-   Simplify the refactoring process</p><p>-   Speed up the integration process</p><p>-   Act as documentation</p><p><br></p><p><br></p><p>&lt;a id="orgbb414ab"&gt;&lt;/a&gt;</p><p><br></p><p>## Basics of unit testing</p><p><br></p><p>Unit testing is just a `=function calling another function=` (or a unit) and checking if the values returned</p><p>match the expected output.</p><p><br></p><p>We have this simple function that normalized an image by dividing all the pixels by 255.</p><p><br></p><p>def _normalize(self, input_image, input_mask):</p><p>""" Normalise input image</p><p>Args:</p><p>input_image (tf.image): The input image</p><p>input_mask (int): The image mask</p><p>Returns:</p><p>input_image (tf.image): The normalized input image</p><p>input_mask (int): The new image mask</p><p>"""</p><p>input_image = tf.cast(input_image, tf.float32) / 255.0</p><p>input_mask -= 1</p><p>return input_image, input_mask</p><p><br></p><p>To make sure that it does exactly what it is supposed to do, we can write `=another function that uses the “_normalize”=`</p><p>and check its result. It will look something like this.</p><p><br></p><p>def test_normalize(self):</p><p>input_image = np.array([[1., 1.], [1., 1.]])</p><p>input_mask = 1</p><p>expected_image = np.array([[0.00392157, 0.00392157], [0.00392157, 0.00392157]])</p><p>result = self.unet._normalize(input_image, input_mask)</p><p>self.assertEquals(expected_image, result[0])</p><p><br></p><p>The `“test_normalize”` function creates a fake input image, calls the function with that image as an argument, and then makes</p><p>sure that the result is equal to the expected image.</p><p><br></p><p>The `“assertEquals”` is a special function, coming from the unittest package in python, it asserts that the two values are equal.</p><p><br></p><p><br></p><p>&lt;a id="org54688cb"&gt;&lt;/a&gt;</p><p><br></p><p>## Unit tests in Python</p><p><br></p><p>The main test framework/runner that comes into Python’s standard library is [unittest](https://docs.python.org/3/library/unittest.html).</p><p><br></p><p>Unittest is pretty straightforward to use and it has only two requirements:</p><p><br></p><p>-   to put your tests into a class</p><p>-   use its special assert functions</p><p><br></p><p>import unittest</p><p>class UnetTest(unittest.TestCase):</p><p>def test_normalize(self):</p><p>. . .</p><p>if __<strong>name</strong>__ == '__<strong>main</strong>__':</p><p>unittest.main()</p><p><br></p><p>**<strong>Important points to notice here</strong>**</p><p><br></p><p>-   In general, test functions are named with `“test_”` as a prefix followed by the name of the function they test.</p><p>-   To run unit tests, we call the “unittest.main()” function which discovers all tests within the module, runs them</p><p>and prints their output.</p><p>-   Another two powerful frameworks are [pytest](https://docs.pytest.org/en/stable/) and [nose](https://nose.readthedocs.io/en/latest/), which are pretty much governed by the same principles.</p><p><br></p><p>**<strong>Tests in Tensorflow</strong>**</p><p>Since we use Tensorflow to program our model we can take advantage of [tf.test](https://www.tensorflow.org/api_docs/python/tf/test)</p><p><br></p><p>import tensorflow as tf</p><p>class UnetTest(tf.test.TestCase):</p><p>def setUp(self):</p><p>super(UnetTest, self).setUp()</p><p>. . .</p><p>def tearDown(self):</p><p>pass</p><p>def test_normalize(self):</p><p>. . .</p><p>if __name__ == '__main__':</p><p>tf.test.main()</p><p><br></p><p><br></p><p>&lt;a id="orgf42bfcf"&gt;&lt;/a&gt;</p><p><br></p><p>## Mocking</p><p><br></p><p>`Mocking` makes it very easy to replace complex logic or heavy dependencies when testing code using `=dummy objects=`.</p><p>By `dummy objects`, we refer to simple, easy to code objects that have the same structure with our real objects but contain</p><p>`fake` or `useless` data.</p><p><br></p><p>In our case a dummy object might be a 2d tensor with all ones which mimics an actual image (just like the “input&lt;sub&gt;image&lt;/sub&gt; in</p><p>the first code snippet).</p><p><br></p><p>Mocking also helps us control the code’s behavior and simulate expensive calls. Let’s look at an example using once again our UNet.</p><p><br></p><p>def load_data(self):</p><p>""" Loads and Preprocess data """</p><p>self.dataset, self.info = DataLoader().load_data(self.config.data)</p><p>self._preprocess_data()</p><p>def _preprocess_data(self):</p><p>""" Splits into training and test and set training parameters"""</p><p>train = self.dataset['train'].map(self._load_image_train, num_parallel_calls=tf.data.experimental.AUTOTUNE)</p><p>test = self.dataset['test'].map(self._load_image_test)</p><p>self.train_dataset = train.cache().shuffle(self.buffer_size).batch(self.batch_size).repeat()</p><p>self.train_dataset = self.train_dataset.prefetch(buffer_size=tf.data.experimental.AUTOTUNE)</p><p>self.test_dataset = test.batch(self.batch_size)</p><p>def _load_image_train(self, datapoint):</p><p>""" Loads and preprocess  a single training image """</p><p>input_image = tf.image.resize(datapoint['image'], (self.image_size, self.image_size))</p><p>input_mask = tf.image.resize(datapoint['segmentation_mask'], (self.image_size, self.image_size))</p><p>if tf.random.uniform(()) &gt; 0.5:</p><p>input_image = tf.image.flip_left_right(input_image)</p><p>input_mask = tf.image.flip_left_right(input_mask)</p><p>input_image, input_mask = self._normalize(input_image, input_mask)</p><p>return input_image, input_mask</p><p>def _load_image_test(self, datapoint):</p><p>""" Loads and preprocess a single test image"""</p><p>input_image = tf.image.resize(datapoint['image'], (self.image_size, self.image_size))</p><p>input_mask = tf.image.resize(datapoint['segmentation_mask'], (self.image_size, self.image_size))</p><p>input_image, input_mask = self._normalize(input_image, input_mask)</p><p>return input_image, input_mask</p><p><br></p><p>What this code does is splitting the data, some shuffling, some resizing, and batching.</p><p>So we want to test this code.</p><p>Everything is nice and well except that freaking loading function.</p><p><br></p><p>self.dataset, self.info = DataLoader().load_data(self.config.data)</p><p><br></p><p>Are we supposed to load the entire data every time we run a single unit test?</p><p>Nah,</p><p><br></p><p>Therefore, we could `mock` that function to return a dummy dataset instead of calling the real one.</p><p>We can do that with unittest&amp;rsquo;s [mock object package](https://docs.python.org/3/library/unittest.mock.html)</p><p>It provides a mock class “Mock()” to create a mock object directly and a “patch()” decorator that replaces</p><p>an imported module within the module we test with a mock object.</p><p><br></p><p>&gt;<em> For those who aren’t familiar, the decorator is simply a function that wraps another function to extend its</em></p><p>&gt;<em> functionality. Once we declare the wrapper function we can annotate other functions to enhance them. See the</em></p><p>&gt;<em> @patch below? That’s a decorator that wraps the “test&lt;sub&gt;load&lt;/sub&gt;&lt;sub&gt;data&lt;/sub&gt;” with the “patch” function. For more information</em></p><p>&gt;<em> follow the link at the end of the post.</em></p><p><br></p><p>By using the “patch()” decorator we get this:</p><p><br></p><p>@patch('model.unet.DataLoader.load_data')</p><p>def test_load_data(self, mock_data_loader):</p><p>mock_data_loader.side_effect = dummy_load_data</p><p>shape = tf.TensorShape([None, self.unet.image_size, self.unet.image_size, 3])</p><p>self.unet.load_data()</p><p>mock_data_loader.assert_called()</p><p>self.assertItemsEqual(self.unet.train_dataset.element_spec[0].shape, shape)</p><p>self.assertItemsEqual(self.unet.test_dataset.element_spec[0].shape, shape)</p><p><br></p><p><br></p><p>&lt;a id="orgb4cdacd"&gt;&lt;/a&gt;</p><p><br></p><p>## Test coverage</p><p><br></p><p>By coverage, we mean how much of our code is actually tested by unit tests.</p><p>Coverage is an invaluable metric that can help us write better unit tests, discover which areas</p><p>our tests don’t exercise, find new test cases, and ensure the quality of our tests.</p><p><br></p><p>coverage run -m unittest /path_to_folder/tests/unet_test.py</p><p>coverage report -m /path_to_folder/tests/unet_test.py</p><p>output--</p><p>Name                          Stmts   Miss    Cover   Missing</p><p>-------------------------------------------------------------</p><p>model/tests/unet_test.py       35      1       97%      52</p><p><br></p><p>This says that we cover 97% of our code. There are 35 statements in total and we missed just 1 of them.</p><p>The missing info tells us which lines of code still need coverage.</p><p><br></p><p><br></p><p>&lt;a id="orgc0eb724"&gt;&lt;/a&gt;</p><p><br></p><p>## Test examples</p><p><br></p><p>Explore some of the different deep learning scenarios and parts of the codebase when unit testing can be incredibly useful.</p><p><br></p><p>**<strong>DATA</strong>**</p><p><br></p><p>-   Ensure that our data has the right format (yes I put it again here for completion)</p><p>-   Ensure that the training labels are correct</p><p>-   Test our complex processing steps such as image manipulation</p><p>-   Assert data completion, quality, and errors</p><p>-   Test the distribution of the features</p><p><br></p><p>**<strong>TRAINING</strong>**</p><p><br></p><p>-   Run a training step and compare the weight before and after to ensure that they are updated</p><p>-   Check that our loss function can be actually used on our data</p><p><br></p><p>**<strong>EVALUATION</strong>**</p><p><br></p><p>-   Having tests to ensure that your metrics ( e.g accuracy, precision, and recall ) are above a threshold when iterating</p><p><br></p><p>over different architectures</p><p><br></p><p>-   You can run speed/benchmark tests on training to catch possible overfitting</p><p>-   Of course, cross-validation can be in the form of a unit test</p><p><br></p><p>**<strong>MODEL ARCHITECTURE</strong>**</p><p><br></p><p>-   The model’s layers are actually stacking</p><p>-   The model’s output has the correct shape</p><p><br></p><p>example:</p><p><br></p><p>def test_ouput_size(self):</p><p>shape = (1, self.unet.image_size, self.unet.image_size, 3)</p><p>image = tf.ones(shape)</p><p>self.unet.build()</p><p>self.assertEqual(self.unet.model.predict(image).shape, shape)</p><p><br></p><p>Define the expected shape, construct a dummy input, build the model, and run a prediction is all it takes.</p><p>Sometimes a few lines of code can save us from a lot of trouble.</p><p><br></p><p><br></p><p>&lt;a id="org0e0c7fa"&gt;&lt;/a&gt;</p><p><br></p><p>## Integration</p><p><br></p><p>These kinds of tests are very powerful tools and aim to test how well our system integrates with other systems.</p><p>We want to be certain that the model returns what the user/client expects in the form that he expects it.</p><p><br></p><p><br></p><p>&lt;a id="org400fe94"&gt;&lt;/a&gt;</p><p><br></p><p>## Conclusion</p><p><br></p><p>-   Unit tests are indeed an invaluable tool in our arsenal, especially when building complex deep learning models.</p><p>-   My laziness has come and bitten me more times than I can count, so I decided to always write them from now on.</p><p>-   But again, we always need to find a balance.</p><p><br></p><p><br></p><p>&lt;a id="orgd3f9aaf"&gt;&lt;/a&gt;</p><p><br></p><p>## References</p><p><br></p><p>-   [Python decorators](https://www.programiz.com/python-programming/decorator)</p><p>-   [Code coverage](https://en.wikipedia.org/wiki/Code_coverage)</p><p>-   [Mocking](https://www.toptal.com/python/an-introduction-to-mocking-in-python)</p><p>-   [Testing in Python](https://realpython.com/python-testing/)</p><p><br></p><p><br></p><p>&lt;a id="orgbb96761"&gt;&lt;/a&gt;</p><p><br></p><p># IN-PROGRESS Logging and Debugging</p><p><br></p><p>source :: &lt;https://theaisummer.com/logging-debugging/&gt;</p><p><br></p><p><br></p><p>&lt;a id="orge9c000a"&gt;&lt;/a&gt;</p><p><br></p><p>## Debug deep learning</p><p><br></p><p><br></p><p>&lt;a id="orgeb14bd2"&gt;&lt;/a&gt;</p><p><br></p><p>## Logging</p><p><br></p><p><br></p><p>&lt;a id="org3e41322"&gt;&lt;/a&gt;</p><p><br></p><p>## Tensorflow logging and debugging</p><p><br></p>]]></content:encoded></item><item><title><![CDATA[A life's journey of a programmer]]></title><description><![CDATA[<pre><code class="language-Python">def hello():
	return "Hello, World!"
    
hello()</code></pre><pre><code class="language-cpp">#include &lt;iostream&gt;

int main() {
	cout &lt;&lt; "Hello, World!" &lt;&lt; "\n";
}</code></pre><pre><code class="language-python">def s:
	return s + "s"</code></pre><!--kg-card-begin: markdown--><pre><code>#include &lt;iostream&gt;

int main() {
	cout &lt;&lt; &quot;Hello, World!&quot; &lt;&lt; &quot;\n&quot;;
}
</code></pre>
<!--kg-card-end: markdown-->]]></description><link>nezubn.com/a-lifes-journey-of-a-programmer/</link><guid isPermaLink="false">6005121456c39f44086f9ae5</guid><dc:creator><![CDATA[Ankit Sharma]]></dc:creator><pubDate>Mon, 18 Jan 2021 04:45:08 GMT</pubDate><content:encoded><![CDATA[<pre><code class="language-Python">def hello():
	return "Hello, World!"
    
hello()</code></pre><pre><code class="language-cpp">#include &lt;iostream&gt;

int main() {
	cout &lt;&lt; "Hello, World!" &lt;&lt; "\n";
}</code></pre><pre><code class="language-python">def s:
	return s + "s"</code></pre><!--kg-card-begin: markdown--><pre><code>#include &lt;iostream&gt;

int main() {
	cout &lt;&lt; &quot;Hello, World!&quot; &lt;&lt; &quot;\n&quot;;
}
</code></pre>
<!--kg-card-end: markdown-->]]></content:encoded></item></channel></rss>